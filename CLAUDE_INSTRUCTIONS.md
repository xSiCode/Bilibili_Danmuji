# Claude 指令记录 — BiliBili_Danmuji 项目

> 本文档记录了开发过程中向 Claude Code 发送的全部指令，按功能模块分类整理。
> 新人可通过本文档学习：① 各模块的业务逻辑；② 如何向 AI 描述需求；③ 调试与迭代的典型流程。

**总计：约 130 条有效指令（已过滤中断、纯反馈类消息）**

**时间跨度：2026-05（早期） → 2026-07-10（最新）**

---

## 目录

1. [观众打分与黑名单系统](#1-观众打分与黑名单系统)
2. [SQLite 数据存储与缓存](#2-sqlite-数据存储与缓存)
3. [后台管理面板](#3-后台管理面板)
4. [实时陌生观众看板](#4-实时陌生观众看板)
5. [账号池与共同关注](#5-账号池与共同关注)
6. [远程访问与网络配置](#6-远程访问与网络配置)
7. [UI/UX 优化](#7-uiux-优化)
8. [性能优化](#8-性能优化)
9. [查阅用户 / AICU 功能](#9-查阅用户--aicu-功能)
10. [代码重构与质量](#10-代码重构与质量)
11. [项目文档](#11-项目文档)

---

## 1. 观众打分与黑名单系统

> 核心业务：对进入直播间的观众进行多维度打分（弹幕、礼物、舰长、关注列表等），根据分数判断是否拉黑。

### 1.1 点赞记录入库
```
需求新增：在 ParseMessageThread.java 处有直播间的点赞记录。要求记录：
点赞人id<uid>、点赞人名字<uname>、被点赞直播间主播id<ruid>、直播间id，写入sqlite。
→ 再新增直播间名字、主播名字列。
```
**日期：** 2026-06-11 | **关键文件：** `ParseMessageThread.java`, SQLite

### 1.2 观众综合记录展示
```
在"实时陌生观众看板"的"签名"栏和"本地黑白名单集"的"打分类型"里追加显示：
该观众在所有直播间的记录结果综述（发言次数、进入次数、送礼次数、点赞记录等）。
```
**日期：** 2026-06-11

### 1.3 本地 HTTP 打分服务
```
在 HttpRoomData.java 处新增一个本地 http 请求，根据返回的 json 结果，
用每一个主播 anchor_uid 来获取打分（分数负=黑，整数=白）。
根据属性值计算最终打分：entry + danmaku + gift + gift_value + guard + guard_value + sc + sc_value；
根据 sessions 计算分裂度。参考 processVisibleFollowingsSync 返回 pair<打分, 打分类型>。
先输出到 testLog，暂不参与实际计算。
```
**日期：** 2026-06-22 | **关键文件：** `HttpRoomData.java`

### 1.4 打分结果接入流程
```
我已测试完毕，将 processLocalSummarySync 的打分结果加入打分流程。
→ 数据结果似乎与其他 http 请求结果冲突，发生了线程数据异常？
→ 目前同时发起 6 个 http 请求，能确保返回数据是同一个 vmid 吗？
→ 把 processLocalSummarySync 外面的 try-catch 放里面，代码不够优雅
→ 同理 asyncHttpGetSameFollowings 外面的 try-catch 也放里面
```
**日期：** 2026-06-22 ~ 2026-06-23

### 1.5 关注列表双页合并
```
关注列表（双页合并）也能合并成一行调用吗？
→ 能把 long total = ... 也放进方法里面吗？
→ 把下面的也简化成一行调用：Pair<Integer, String> sameResult = ...
```
**日期：** 2026-06-23

### 1.6 打分阈值配置化
```
把刚才的 score < 0 改为根据实际配置的拉黑值。
→ 是否可以把 processLocalSummarySync 的返回结果添加到 getBlackWhiteList 里的 scoreType？
```
**日期：** 2026-06-23

### 1.7 拉黑按钮显示 Bug
```
在陌生观众看板里，有一定几率打分为正但显示"解除拉黑"按钮，应该显示"拉黑"按钮。
→ 太棒了，问题解决了
```
**日期：** 2026-06-23

---

## 2. SQLite 数据存储与缓存

> 将原来基于 CSV 的数据存储迁移到 SQLite，提升查询性能和多维度分析能力。

### 2.1 API 响应缓存
```
在 processFollowings 方法里有 5 个 http 请求，参数主要是 vmid。是否可以通过 id 和 api 返回结果
来做 sqlite 缓存？如果 vmid 已存在，直接返回缓存结果。
→ sqlite 的过期时间改为 30 天，数据都放一张表。
→ 是缓存所有的 http 结果，不是只缓存 code:0 的。
→ 限流的响应不缓存，只缓存成功和错误。
```
**日期：** 2026-06-16 | **关键文件：** `HttpRoomData.java`

### 2.2 缓存淘汰策略
```
现在的清除机制是 30 天或 200,000 条，效率不高。应增加触发次数统计，
优先清理只触发过一次的记录，次数越高越晚清理。
→ 去掉 ttl 功能，只通过条数和次数来清除。超过条数阈值时删除命中次数=1 的记录。
```
**日期：** 2026-06-16

### 2.3 SQLite Schema 变更
```
刚才执行了 DROP INDEX IF EXISTS idx_api_cache_expire，是否导致 api_cache 表没更新？
→ 没有看到 hit_count 列。
```
**日期：** 2026-06-16

### 2.4 CSV → SQLite 迁移
```
后台管理会根据条件从 csv 读取历史数据，把数据源从 csv 改为 sqlite 是否有性能提升？
→ 开始！
→ 现在所有的 csv 文件都只是写入文件，不会读取到内存吗？
→ 后台管理界面功能默认都从 sqlite 读取吗？
→ 导入导出继续保持 csv 功能。实时陌生观众看板也要从 sqlite 默认读取。
→ 本次修改会破坏数据的持久化吗？我只需要把数据源从 csv 切换到 sqlite。
```
**日期：** 2026-06-19

### 2.5 足迹留印数据
```
在本次需求中是否使用了 sqlite 中"足迹留印"记录的数据？
→ 需要增加进去作为第 8 个事件类型（简略的观众进入记录），show=5 保持不变。
```
**日期：** 2026-06-11

---

## 3. 后台管理面板

> 包含直播间管理、弹幕管理、观众管理、匹配管理、关注管理、礼物管理共 6 个数据面板。

### 3.1 数据源切换问题
```
匹配管理和关注管理页面默认似乎什么都不读取。我需要默认从 sqlite 按条件筛选（主要是直播间和时间），
但也可以从 csv 导入。
→ 有点幽默了，这 3 个界面正常了，另外 3 个又有问题了。
→ 有选项但选完后不加载数据？直播间管理、弹幕管理、观众管理都没有图表。
```

### 3.2 文件选择器逻辑
```
七个管理页面中，既然默认从 sqlite 读取，选择文件里的 csv 应置空，不应读取默认的当前直播间 csv。
换个角度：csv 处没有文件时，筛选应对应数据库里的表文件。
```

### 3.3 时间过滤缺失
```
弹幕管理和礼物管理似乎没有按时间过滤？
```

### 3.4 文件路径 Bug
```
java.io.FileNotFoundException: C:\Users\xSiCo\Documents\Danmuji_log (拒绝访问。)
```

### 3.5 陌生观众看板 debug（耗时最长的 Bug）
```
陌生观众看板和直播间管理有问题，没有数据、没有表格、没有图表。
→ 很棒，现在只剩这两个问题了...
→ 很棒，现在只剩这 1 个问题了：陌生观众看板。
→ 可能是跟开始时间和结束时间都没有默认值有关吧。
→ 可以参考"直播间管理"来修改，将开始时间写入"2026/06/19"。
→ 不，还是要优先用最近 3 小时。
→ 第一次能正常显示数据，但是后续就不行了。
→ 很奇怪，出问题的时候，开始时间和结束时间从具体数值变成了"yyyy/mm/日"。
→ 问题就是出在"yyyy/mm/日"。
→ 既然第一次能正常显示和过滤，切换出去时做状态保存。
→ 第一次切出去再回来正常，第二次及以后有问题。
→ 呵呵，现在一次都没成功了。
→ 你要不加点日志再调试。
→ 正常时看板页面是独占的，出问题时看板页面还有其他 3 个标签。
→ （粘贴调试日志...）
→ 复现之后没有日志打印。
→ 现在正常了，谢谢你！
```
**日期：** 2026-06-19（连续调试约 2 小时） | **教训：** 前端 tab 切换时状态管理要特别注意，时间格式的默认值和回填逻辑需要一致。

---

## 4. 实时陌生观众看板

> 实时展示进入直播间的陌生人（非粉丝/非勋章）观众信息。

### 4.1 时间排序与分页
```
为什么"实时陌生观众看板"列表一页只显示 6 行新观众？应该逐渐增加到 20 行再新增一页。
→ 没修复，加调试日志。
→ （粘贴日志）response total=746 totalPages=38 rows.length=6 currentPage=38
→ 还是按时间升序排较好，最新的在尾页。
→ 还是有问题，页数一直是 20 页，应该可以增加（新观众进来）。
```
**日期：** 2026-06-20

### 4.2 列宽与布局
```
各个列的宽度要跟随列属性，不是固定位置。现在场次列就宽得很。
→ 观众名列改小一点，改成 140px，标题和属性要在同一列。
```

### 4.3 列标题顺序 Bug
```
列标题名顺序不对，列的属性值一直都是对的。
```

### 4.4 时间显示格式
```
时间列显示的是年月日，隐藏了时分秒。我需要显示时分秒。
→ 不对，需要显示全部时间。浏览器宽度有限时先显示时分秒，光标悬浮时显示完整时间。
→ 光标放到观众名上时，如果名字没显示完全则显示完全。
```
**日期：** 2026-06-20

---

## 5. 账号池与共同关注

> 多账号管理系统，用于轮询调用 API 以突破频率限制。

### 5.1 共同关注 API 集成
```
找到两个 api：用户动态信息、获取共同关注的 up 主。请帮我写上这两个 api 的调用，
结果写入 testLog.txt。
→ 用户动态 api 限制太大，已去掉。共同关注 api 只返回用户 ID 和用户名。
```

### 5.2 账号池复选框
```
在账号列表里新增一列"共同关注"勾选框，勾选后使用该账号发起共同关注 api 请求。
→ 主账号不能勾选？不需要有此限制。
→ 不对，是主账号也要有复选框，所有账号都要有复选框。
```

### 5.3 请求与账号对应关系
```
共同关注 api 请求，实际请求的账号和勾选的账号不是对应的。
当勾选多个账号时，是轮询发起请求；只勾选一个时，应用该账号发起请求。
→ 从原来勾选多个变为勾选一个，还会出问题。
→ 共同关注请求就不要使用缓存了。
→ CookiePool: 共同关注-无可用账号(子=0 主可用=false)，回退主账号（但我有勾选账号的）。
```

### 5.4 冷却机制
```
卡片 api 和动态 api 是共用的同一个冷却吗？
→ 错了！关注列表和勋章墙不需要冷却。卡片冷却 5 分钟，动态冷却 15 分钟。
→ 共同关注的冷却时间是 10 分钟。
→ 三个冷却时间都是独立的吗？
→ 我似乎没有看到共同关注被限流后的冷却处理。
→ 看来它的冷却处理相比另外两个要提前了。
```

### 5.5 持久化兼容性
```
现在账号池新增了一项，还兼容老的持久化数据格式吗？
```

### 5.6 打分权重调整
```
之前关注数大于 100 时单独处理，容易漏掉签名判断。
现在改为关注数大于 100 时是增加处理，共同关注分和原来打分相加（重叠分数可以接受），
做到分值绝对值变大的效果。
→ 账号池的共同关注默认不勾选。当有勾选动作时，才将该选项的值持久化。
→ 如果一个都没有勾选，是否可以拦截请求发送？
→ 共同关注绑定了关注列表的正常打分，他们应该没有强绑定才是。
```

---

## 6. 远程访问与网络配置

> 将本地项目暴露到局域网，支持手机等设备访问。

### 6.1 可移植性评估
```
审阅项目代码，核心需求是记录直播间的观众动作后打分，然后判断是否拉黑。
判断项目是否可以移植到 Android、iOS 和鸿蒙系统上。给出高性价比方案。
```
**日期：** 2026-06-16 | **结论：** 不适合移植，改为本地 Windows + WebUI 远程访问。

### 6.2 本地远程访问
```
我决定改为将项目运行在本地 Windows 上，通过 ip+端口访问页面。
→ 改了之后影响本地访问吗？外网是否有拦截？支持 IPv6 吗？
→ 直接实施。
```

### 6.3 防火墙配置
```
怎样针对本应用进行防火墙打开？
→ 远程访问是 tcp 还是 udp？
→ udp 一般用在哪些场景？http、websocket 都是 tcp 吗？
```

### 6.4 局域网访问问题
```
我在局域网输入 192.168.0.105:12260 却不能访问，http://localhost:12261/ 可以打开。
→ 怎样判断指定端口是否可以被局域网内其他浏览器访问？
→ 已经可以访问了，是我忘了输入 http。
```

### 6.5 HTTPS 与 Nginx
```
能否支持 https 的方式访问？
→ 远程访问是否必须要 https？
→ nginx 代理本项目的思路是什么？我对 nginx 不了解。
```

---

## 7. UI/UX 优化

> WebUI 界面调整，包括移动端适配、布局优化等。

### 7.1 移动端适配
```
在手机端访问时，一些没显示完全的文字再也看不到了，因为手机没有光标。
能否增加一个点击未显示完全字段就会显示完全的功能？
→ 在移动端点击后会在当前多行显示，这很不优雅。改为在当前位置弹窗显示，
  不影响行高度，有新用户进入时不要关闭该弹窗。
```
**日期：** 2026-06-16

### 7.2 本地黑白名单姬 UI
```
在"本地黑白名单姬"里，打分类型改为靠左排列（非水平居中），交换显示打分和次数列。
```
**日期：** 2026-06-18

### 7.3 列宽固定
```
观众名列改小一点，改成 140px，标题和属性要在同一列。
```

### 7.4 时间对齐
```
时间显示需要优先显示右边内容，text-align: right 不生效怎么办？
```

---

## 8. 性能优化

> 针对内存占用过大、多开场景的优化。

### 8.1 内存优化
```
项目在 Windows 运行时内存占用 480M-550M。我需要多开 15 个本项目，能否减少内存占用？
→ 堆内存 32-96MB 会不会太小？
→ 使用保守方案。不需要写多开脚本。开始优化。
→ 还真有用，这会不会影响数据保存？
```
**日期：** 2026-06-19 | **方案：** JVM 参数 `-Xms64m -Xmx128m -XX:+UseSerialGC` + 线程池优化 + Spring Boot 配置瘦身

### 8.2 CSV → SQLite 迁移的性能收益
```
你是对的。我看后台管理会根据条件从 csv 读取数据，把数据源从 csv 改为 sqlite 会有性能提升吗？
→ 开始！
```

### 8.3 删除后台管理功能
```
如果我把"后台管理"这个功能删除，能否提升性能？（答：不必要，实际不打开页面就无负载）
```

---

## 9. 查阅用户 / AICU 功能

> 第三方平台用户数据查询与缓存。

### 9.1 查阅用户事件过滤
```
在"查阅用户"里，点击"事件类型"可以选择按事件类型过滤；
搜索框支持详情搜索；显示列表改为 20 行表格，页数不限，时间范围固定 2 小时。
→ 要支持实时显示和搜索功能，不做功能删减。
→ 增加时间段过滤，默认最近两小时。
```

### 9.2 列布局调整
```
观众名字列和直播间列交换位置。直播间宽度固定为 100px，观众名字列固定为 150px，剩余给详情列。
→ 搜索框也改为类型搜索：uid, 名字, 直播间, 详情。
→ 添加按钮，可选择搜索指定的类型。
→ 新增直播间搜索范围选择，可以多选和单选。
→ 现在功能失效了。
→ 选中项使用高亮蓝色，不是灰色。
→ 将控件都放到同一行。
→ 直播间列表高度再增高点，全选按钮放到"直播间名字"后边。
→ 搜索范围默认选择"详情"。
```
**日期：** 2026-06-27

---

## 10. 代码重构与质量

> 代码质量改进、重构、代码审阅。

### 10.1 代码审阅
```
审阅当前会话的代码修改，是否有考虑不周到的地方或不足之处？
```
**日期：** 2026-06-20

### 10.2 UI 重构
```
查看我两个 html 文件的修改，实例的列数据并没有随标题的改变而改变。
```

### 10.3 try-catch 代码优化
```
把 processLocalSummarySync 外面的 try-catch 放里面，代码不够优雅。
→ 同理 asyncHttpGetSameFollowings 外面的 try-catch 也放里面。
→ 关注列表（双页合并）也能合并成一行调用吗？
→ 把 long total = ... 也放进方法里面。
→ 把下面的也简化成一行调用。
```

### 10.4 代码回退恢复
```
刚才不小心回退了，你再改一次。
```
**日期：** 2026-06-23

---

## 11. 项目文档

> 项目文档编写与维护。

### 11.1 README 重写
```
整理项目功能，将使用方法和注意事项写入 readme 文档（之前的删除），便于交接给新人使用。
```
**日期：** 2026-07-10

### 11.2 本指令文档
```
将本项目我发送过的 claude 指令转存到合适的文档，便于新人学习。
```
**日期：** 2026-07-10

---

## 附录：与 Claude 高效协作的技巧

基于 130+ 条指令的实战经验：

### A. 需求描述
- **明确文件位置**：`在 xyz/acproject/danmuji/http/HttpRoomData.java:846 处新增...`
- **给出数据格式**：`记录点赞人id<uid>，点赞人名字<uname>`
- **指明参考代码**：`参考 processVisibleFollowingsSync 来返回 pair<打分, 打分类型>`
- **说明生效范围**：`暂不参与实际运行时的计算，先输出到 testLog`

### B. 调试反馈
- **贴日志是最有效的**：复制完整错误栈或前端 console 日志
- **描述复现步骤**：`第一次切换出去再回来正常，第二次及以后有问题`
- **指出具体差异**：`正常时页面是独占的，出问题时页面还有其他 3 个标签`

### C. 迭代节奏
- **小步迭代**：一次只改一个模块，验证后再改下一个
- **及时反馈**：`现在正常了，谢谢你` / `还是有问题，xxx`
- **清理代码**：`把 try-catch 放里面，代码不够优雅`

### D. 关键字指南
| 场景 | 推荐指令模式 |
|------|-------------|
| 新功能 | `需求新增：在 X 文件处做 Y 功能，数据格式是 Z` |
| 修改功能 | `需求变更：原来 A，现在改为 B，因为 C` |
| Debug | `X 功能有问题，现象是 Y，日志如下：...` |
| 代码审阅 | `审阅当前会话的代码修改，是否有不足之处？` |
| 性能优化 | `内存占用 X MB，需要降到 Y MB 以内，给出方案` |
| 确认执行 | `直接实施` / `开始` / `需要` |
