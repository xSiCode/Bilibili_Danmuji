# 后台管理功能开发总结

## 一、直播间管理

### CSV 格式
- 文件：`{roomId}_{anchorName}_1_直播间信息.csv`，位于 `Danmuji_log/` 目录
- 列：`时间,观看数,在线数,点赞数`（格式 `yyyy-MM-dd HH:mm`，UTF-8 with BOM，每 3 分钟一条）

### 功能清单

| 功能 | 描述 |
|------|------|
| 文件选择 | 默认读取当前直播间 CSV，下拉框可切换其他直播间文件 |
| 表格展示 | 每页 10 行，格式与 CSV 一致，含分页（首页/尾页/跳转） |
| 列拖曳 | SortableJS 实现，仅视觉生效，不影响保存 |
| 时间筛选 | `datetime-local` 控件，精确到分钟，默认使用文件首尾时间 |
| 导出 CSV | 按筛选条件导出下载 |
| 删除行 | 单行删除并实时保存（原子写入 `.tmp` → `move`） |
| 观看数折线图 | Chart.js 折线图，X 轴 HH:mm，悬停显示完整时间+数值 |
| 在线数折线图 | 同上 |
| 点赞数折线图 | 同上 |
| 统计卡片 | 累计观看数 / 累计点赞数 / 平均在线·最高在线 / 总观看时长·人均观看 |

### 统计计算规则
- 累计观看数 = 筛选范围内观看数最大值
- 累计点赞数 = 筛选范围内点赞数最大值
- 总观看时长 = Σ(每个时间点的在线数 × 60 秒)（实际采集间隔 60 秒）
- 人均观看时长 = 总观看时长 ÷ 累计观看数

---

## 二、弹幕管理

### CSV 格式
- 文件：`{roomId}_{anchorName}_2_弹幕信息.csv`，位于 `Danmuji_log/` 目录
- 列：`发送时间,id,名字,弹幕`（格式 `yyyy-MM-dd HH:mm:ss`，含 CSV 引号转义，UTF-8 with BOM）

### 功能清单

| 功能 | 描述 |
|------|------|
| 文件选择 | 默认读取当前直播间 CSV，下拉框可切换 |
| 表格展示 | 每页 10 行，分页（首页/尾页/跳转） |
| 列拖曳 | SortableJS，仅视觉生效 |
| 时间筛选 | 精确到分钟，默认使用文件首尾时间 |
| 全文搜索 | 搜索所有列内容，回车触发 |
| 导出 CSV | 按筛选+搜索条件导出 |
| 弹幕数量折线图 | 每分钟桶内弹幕数量趋势，X 轴 HH:mm |
| 发送排行榜柱状图 | Top 5 发送者，横向柱状图（Y=名字，X=发送数） |
| 弹幕质量折线图 | 质量 = 时间段内弹幕总长度 ÷ 弹幕数量（每分钟桶），单线图 |
| 词频柱状图 | Top 15 高频词，竖向柱状图（X=词，Y=出现次数） |
| 统计卡片 | 发弹幕人数（去重 UID）/ 弹幕数量 / 弹幕总字数 / 平均弹幕长度 |

### 统计计算规则
- 发弹幕人数 = 筛选范围内去重 UID 数量（排除 uid=0）
- 弹幕总数 = 筛选范围内行数
- 弹幕总字数 = 所有弹幕内容字符数之和
- 词频 = 按非中文/非字母数字字符分词，长度 ≥2 的 token 计数，取 Top 30
- 时间桶粒度 = 固定 1 分钟

---

## 三、观众管理

### CSV 格式
- 文件：`{roomId}_{anchorName}_4_观众信息.csv`，位于 `Danmuji_log/` 目录
- 列：`最近,id,观众,打分,打分类型,次数,判定表,场次`（格式 `yyyy-MM-dd HH:mm:ss`，UTF-8 with BOM）

### 功能清单

| 功能 | 描述 |
|------|------|
| 文件选择 | 默认读取当前直播间 CSV，下拉框可切换 |
| 表格展示 | 每页 10 行，分页（首页/尾页/跳转） |
| 列拖曳 | SortableJS，仅视觉生效 |
| 列排序 | 点击表头切换升序/降序，默认排序：时间↑ → id↑ |
| 时间筛选 | 精确到分钟，开始时间 = min(开播时间, CSV 首行时间)，结束时间 = CSV 末行时间 |
| 全文搜索 | 搜索所有列内容，回车触发 |
| 导出 CSV | 按筛选+搜索条件导出 |
| 观众数量折线图 | 每分钟进场人数（`次数` 列求和），X 轴 HH:mm |
| 打分人数分布柱状图 | X=分数，Y=该分数对应人数 |
| 进出榜柱状图 | Top N（可配置 1~人数/2），X=名字，Y=次数（`次数` 列求和），次数相同按时间降序 |
| 场次榜柱状图 | Top N，X=名字，Y=场次（`场次` 列求和），场次相同按时间降序 |
| 进出频次柱状图 | X=次数值，Y=该次数出现的人数频次 |
| 场次频次柱状图 | X=场次值，Y=该场次出现的人数频次 |
| 排名数量可调 | 输入框指定进出榜/场次榜数量（1~人数/2），点击应用刷新 |
| 统计卡片 | 进场人数（次数求和）/ 实际人数（行数）/ 平均每分钟人数 / 打分总和·均分 / 判定表是·否数量 / 当前排序状态 |

### 统计计算规则
- 进场人数 = Σ(所有行的 `次数` 列)，`次数` 解析失败时回退 +1
- 实际人数 = 筛选范围内行数
- 平均每分钟人数 = 进场人数 ÷ 时间跨度（分钟）
- 打分总和 = Σ(所有行的 `打分` 列)，平均值 = 总和 ÷ 实际人数
- 判定表 = 统计"是"/"否"数量
- 进出榜 = 按人聚合 `次数` 列求和，降序排列，次数相同按最近时间降序
- 场次榜 = 按人聚合 `场次` 列求和，降序排列，场次相同按最近时间降序
- 进出频次 = 统计每个"次数"值出现的人数（频次分布直方图）
- 场次频次 = 统计每个"场次"值出现的人数（频次分布直方图）
- 时间桶粒度 = 固定 1 分钟

---

## 四、匹配管理

### CSV 格式
- 文件：`{roomId}_{anchorName}_5_匹配信息.csv`，位于 `Danmuji_log/` 目录
- 列：`最近匹配,匹配id,匹配名,匹配分,匹配次数`（格式 `yyyy-MM-dd HH:mm:ss`，UTF-8 with BOM）

### 功能清单

| 功能 | 描述 |
|------|------|
| 文件选择 | 默认读取当前直播间 CSV，下拉框可切换 |
| 表格展示 | 每页 10 行，分页（首页/尾页/跳转） |
| 列拖曳 | SortableJS，仅视觉生效 |
| 列排序 | 点击表头切换升序/降序，默认排序：匹配次数↓ |
| 时间筛选 | 精确到分钟，默认使用文件首尾时间 |
| 全文搜索 | 搜索所有列内容，回车触发 |
| 导出 CSV | 按筛选+搜索条件导出 |
| 匹配频次榜柱状图 | Top 15，横向柱状图（Y=匹配名，X=匹配次数） |
| 匹配分数分布柱状图 | 竖向柱状图（X=匹配分，Y=该分数对应的数量） |
| 统计卡片 | 匹配人数 / 匹配总次数 / 匹配总分·均分 |

---

## 五、通用技术实现

### 后端架构
- 所有 API 位于 `WebController.java`，使用 `@ResponseBody` + `Response.success()` 统一返回格式
- CSV 解析使用引号感知的 `parseCsvLine()` 方法（处理字段内含逗号/引号/换行）
- 文件操作使用 UTF-8 + BOM，写入采用原子模式（`.tmp` → `Files.move`）
- 路径安全校验 `validateFilePath()` 防止目录穿越

### 前端架构
- 图表：Chart.js 4.4.7 CDN
- 列拖曳：SortableJS 1.15.6 CDN
- UI 框架：Bootstrap 5 + jQuery 3.7
- 所有 tab-pane 懒加载（switchTab 触发首次数据请求）
- 时间筛选控件：`<input type="datetime-local">`，值格式 `yyyy-MM-ddTHH:mm` ↔ `yyyy-MM-dd HH:mm`

### 后端 API 端点汇总

| 模块 | list | read | export | statistics | import | delete |
|------|------|------|--------|------------|--------|--------|
| 直播间 | `/listCsvFiles` | `/readCsvData` | `/exportFilteredCsv` | `/getCsvStatistics` | `/importCsvFile` | `/deleteCsvRow` |
| 弹幕 | `/listBarrageCsvFiles` | `/readBarrageCsvData` | `/exportBarrageFilteredCsv` | `/getBarrageStatistics` | `/importBarrageCsvFile` | `/deleteBarrageCsvRow` |
| 观众 | `/listVisitorCsvFiles` | `/readVisitorCsvData` | `/exportVisitorFilteredCsv` | `/getVisitorStatistics` | `/importVisitorCsvFile` | `/deleteVisitorCsvRow` |
| 匹配 | `/listMatchCsvFiles` | `/readMatchCsvData` | `/exportMatchFilteredCsv` | `/getMatchStatistics` | `/importMatchCsvFile` | — |
