# Bilibili_Danmuji 高并发弹幕性能问题分析报告

## 场景说明

基于用户实际场景：**每秒约 10 人进入直播间 + 每秒约 3 条弹幕**（加上礼物/关注/欢迎/系统消息，总计约 15-30 条消息/秒）。

---

## 问题 1（致命）：主处理线程内同步发起 HTTP 请求 — 直接阻塞所有消息处理

**文件**: `src/main/java/xyz/acproject/danmuji/thread/core/ParseMessageThread.java:1106`

**现象**: 每当有访客进入直播间（`msg_type == 1`），在主消息处理线程中同步调用 `HttpUserData.httpGetUserCardInfo(_uid)` 获取用户详细信息。这个方法内部发起了 **5 次串行 HTTP 请求**：
1. B站空间信息接口 (`x/space/acc/info`) — 约 100-300ms
2. 关注列表可见性检查 (`x/relation/followings`) — 约 100-300ms
3. 粉丝列表可见性检查 (`x/relation/followers`) — 约 100-300ms
4. 关注列表检查（第二处）(`x/relation/followings`) — 约 100-300ms
5. 用户卡片接口 (`x/web-interface/card`) — 约 100-300ms

**影响**: 每个访客进入时，主处理线程被阻塞 **500-1500ms** 等待 HTTP 响应。按每秒 10 人进入计算，主线程将 100% 的时间都在等待 HTTP，**弹幕、礼物、关注等所有其他消息完全无法处理**。这是当前架构下最致命的性能瓶颈。

**修改方法**: 将 `httpGetUserCardInfo` 调用彻底移出主处理线程：
```java
if (getCenterSetConf().is_watcher_log()) {
    final long _uid = interact.getUid();
    final String _uname = interact.getUname();
    // 第一步：先用已有数据快速构建日志
    stringBuilder.append(...);
    // 第二步：异步获取用户详情
    WATCHER_EXECUTOR.execute(() -> {
        JSONObject card = HttpUserData.httpGetUserCardInfo(_uid);
        // 构建完整日志后写入
    });
}
```

---

## 问题 2（致命）：主线程中调用 Runtime.exec() 打开浏览器

**文件**: 
- `src/main/java/xyz/acproject/danmuji/thread/core/ParseMessageThread.java:1179`
- `src/main/java/xyz/acproject/danmuji/http/HttpRoomData.java:492`

**现象**: 当检测到"可疑用户"（无头像、低等级、疑似人机等）时，在消息处理主线程中直接调用：
```java
Runtime.getRuntime().exec(new String[]{"cmd", "/c", "start", "", url+midStr});
```

**影响**: `Runtime.exec()` 是一个极其昂贵的系统调用，会 fork 新进程并启动浏览器，耗时可能 **500-3000ms**。这个调用发生在主处理线程中，一旦触发，整个消息处理管线完全停滞。

**修改方法**: 
1. 将浏览器打开改为仅记录日志，需要时手动打开
2. 或使用 `Desktop.browse()` 异步打开（但仍是独立进程）
3. 最建议：改为写入一个"待查用户"日志文件，用户自行查看后手动打开

---

## 问题 3（致命）：每条进入消息创建新线程 + 内部含大量 HTTP 请求

**文件**: `src/main/java/xyz/acproject/danmuji/thread/core/ParseMessageThread.java:1209-1211`

```java
new Thread(() -> {
    HttpRoomData.processFollowings(_follow_uid, _follow_uname);
}).start();
```

**影响**: 每秒 10 个新线程被创建。`processFollowings` 内部：
- 发起 HTTP 请求获取关注列表（最多 5 页 × 50 条 = 250 条数据）
- 含另一个 `Runtime.exec()` 调用（HttpRoomData.java:492），检测到"野猪"用户时又打开浏览器

每秒 10 个线程的创建/销毁 + 大量 HTTP 请求 + 可能的浏览器进程，会快速耗尽系统资源。

**修改方法**: 
```java
// 使用固定大小的线程池
private static final ExecutorService FOLLOW_EXECUTOR = 
    new ThreadPoolExecutor(2, 4, 60L, TimeUnit.SECONDS,
        new LinkedBlockingQueue<>(100),
        new ThreadPoolExecutor.DiscardOldestPolicy());

// 替换 new Thread
FOLLOW_EXECUTOR.execute(() -> {
    HttpRoomData.processFollowings(_follow_uid, _follow_uname);
});
```
同时将 `processFollowings` 中的 `Runtime.exec()` 移除或改为异步。

---

## 问题 4（严重）：单线程串行处理所有消息

**文件**: `src/main/java/xyz/acproject/danmuji/thread/core/ParseMessageThread.java:87`

**现象**: 整个消息处理是单一的 `while(!FLAG)` 循环，消息被逐条处理。即使没有问题 1 的 HTTP 阻塞，所有消息（弹幕、礼物、关注、欢迎、上舰、醒目留言等）都在一个线程中串行处理。

**影响**: 结合问题 1，即使没有 HTTP 请求，单线程在 30 msg/s 下勉强可处理。但问题 1 的 HTTP 阻塞让这个架构完全失效。修复问题 1 后，单线程在 30 msg/s 下可胜任，但未来流量增长时仍会是瓶颈。

**修改方法**: 将消息类型判断（快速操作）和具体业务处理（耗时操作）分离：
```java
// 主循环只做 JSON 解析和 cmd 路由
cmd = parseCmd(jsonObject.getString("cmd"));
switch (cmd) {
    case "DANMU_MSG":
        DANMU_EXECUTOR.execute(() -> handleDanmu(jsonObject));
        break;
    case "INTERACT_WORD_V2":
        INTERACT_EXECUTOR.execute(() -> handleInteract(jsonObject));
        break;
    // ...
}
```

---

## 问题 5（中等）：Vector 作为全局队列的锁竞争

**文件**: `src/main/java/xyz/acproject/danmuji/conf/PublicDataConf.java:103-122`

**现象**: 7 个 `Vector` 用作生产者-消费者队列。`Vector` 每个方法都是 `synchronized` 的，`get(0)` + `remove(0)` 是两个独立加锁操作，存在竞态。

**影响**: 在 15-30 msg/s 下影响有限，但与问题 4 结合时，每个消息处理中会多次操作 Vector（取出消息 → 处理后放入 logString/replys/barrageString），每次都有锁开销。

**修改方法**: 替换为 `LinkedBlockingQueue`：
```java
public static final LinkedBlockingQueue<String> resultStrs = new LinkedBlockingQueue<>(5000);
public static final LinkedBlockingQueue<String> logString = new LinkedBlockingQueue<>(500);
// ...其他类似
```

---

## 问题 6（中等）：synchronized wait/notify 的低效通知

**文件**: 多处，包括：
- `ParseMessageThread.java:89-92, 113-116, 237-239, 1488-1496`
- `SendBarrageThread.java:84-91`
- `LogThread.java:36-43`

**现象**: 每条消息触发 3-4 次 `synchronized(thread) { thread.notify(); }` 来唤醒日志线程、自动回复线程、发送弹幕线程。

**影响**: 每条消息有 3-4 次监视器锁获取/释放。配合问题 5 改用 `BlockingQueue` 后，这些手动 wait/notify 可全部消除。

**修改方法**: 改用 `BlockingQueue.take()` 和 `BlockingQueue.put()` 后自然消除。

---

## 问题 7（中等）：日志文件每次写入都打开/关闭

**文件**: `src/main/java/xyz/acproject/danmuji/tools/file/LogFileTools.java:39-113`

**现象**: `logFile()` 每次写入一行日志都执行：打开文件 → 创建流 → 写一行 → flush → close。

**影响**: 每秒 15-30 次文件打开/关闭操作（弹幕日志 + 关注日志 + 观众日志），造成不必要的磁盘 I/O。

**修改方法**: 使用批量写入或保持流打开：
```java
private static final LinkedBlockingQueue<String> logQueue = new LinkedBlockingQueue<>(10000);
static {
    Thread flusher = new Thread(() -> {
        List<String> batch = new ArrayList<>();
        while (true) {
            logQueue.drainTo(batch, 500);
            if (!batch.isEmpty()) {
                try (BufferedWriter bw = new BufferedWriter(new FileWriter(file, true))) {
                    for (String msg : batch) { bw.write(msg); bw.newLine(); }
                }
                batch.clear();
            }
            Thread.sleep(1000);
        }
    });
    flusher.setDaemon(true);
    flusher.start();
}
```

---

## 问题 8（中等）：OkHttpClient 无连接池配置

**文件**: `src/main/java/xyz/acproject/danmuji/utils/OkHttp3Utils.java:34-39`

```java
private OkHttp3Utils() {
    OkHttpClient.Builder builder = new OkHttpClient.Builder();
    builder.readTimeout(READ_TIMEOUT, TimeUnit.SECONDS);
    builder.connectTimeout(CONNECT_TIMEOUT, TimeUnit.SECONDS);
    builder.writeTimeout(WRITE_TIMEOUT, TimeUnit.SECONDS);
    okHttpClient = builder.build();  // 无连接池配置
}
```

**影响**: OkHttp 默认连接池只保持 5 个空闲连接，空闲 5 分钟后关闭。在访客进入时同时发起多个 HTTP 请求（问题 1 中的 5 次请求 + 关注列表请求），连接不够用时会频繁建立新 TCP 连接，增加延迟。

**修改方法**: 显式配置连接池：
```java
OkHttpClient.Builder builder = new OkHttpClient.Builder();
builder.connectionPool(new ConnectionPool(20, 5, TimeUnit.MINUTES));
builder.readTimeout(READ_TIMEOUT, TimeUnit.SECONDS);
builder.connectTimeout(CONNECT_TIMEOUT, TimeUnit.SECONDS);
builder.writeTimeout(WRITE_TIMEOUT, TimeUnit.SECONDS);
okHttpClient = builder.build();
```

---

## 问题 9（低）：DanmuWebsocket 同步阻塞广播

**文件**: `src/main/java/xyz/acproject/danmuji/controller/DanmuWebsocket.java:56-63`

```java
for(DanmuWebsocket ws : webSocketServers) {
    synchronized (ws.session) {
        ws.session.getBasicRemote().sendText(message);
    }
}
```

**影响**: 对每条弹幕，遍历所有浏览器客户端同步发送。如果某个浏览器标签页网络慢，会拖慢所有客户端的消息推送。

**修改方法**: 使用异步发送：
```java
for (DanmuWebsocket ws : webSocketServers) {
    if (ws.session.isOpen()) {
        ws.session.getAsyncRemote().sendText(message);
    }
}
```

---

## 问题 10（低）：ParseThankGiftThread 忙等循环

**文件**: `src/main/java/xyz/acproject/danmuji/thread/ParseThankGiftThread.java:66`

```java
Thread.sleep(20);  // 每20ms唤醒一次检查时间
```

**影响**: 线程每 20ms 唤醒一次，产生不必要的上下文切换。每秒 50 次无意义的线程调度。

**修改方法**: 使用 `wait(timeout)` 精确等待到延迟结束：
```java
synchronized (this) {
    long waitTime = getDelaytime() - (System.currentTimeMillis() - getTimestamp());
    if (waitTime > 0) this.wait(waitTime);
}
```

---

## 问题 11（低）：系统调用 System.out.println()

**文件**: `ParseMessageThread.java` 中大量 `System.out.println(stringBuilder.toString())`

**影响**: `System.out.println()` 内部是 `synchronized` 的，开启控制台模式时会与其他写入控制台的线程竞争。

**修改方法**: 使用 Log4j2 AsyncLogger 代替。

---

## 问题 12（低）：AutoReplyThread 包含同步 HTTP/API 调用

**文件**: `src/main/java/xyz/acproject/danmuji/thread/AutoReplyThread.java`

**现象**: 自动回复模板中的某些占位符会触发同步 HTTP 请求：
- `%LIVETIME%` → 调用 `HttpRoomData.httpGetRoomInit()` (HTTP 请求)
- `%WATHER%` / 天气相关 → 调用 `apiService.getWeatherV2()` (HTTP 请求)
- `%PC_RP_DFEN%` 等 Apex 相关 → 调用 `apiService.getApexPredator()` (HTTP 请求)
- `%MAKER_DAY1%` 等 → 调用 `apiService.getApexMessage()` (HTTP 请求)

**影响**: 如果自动回复规则中使用了这些占位符，每条匹配的弹幕都会触发 API 调用。在 AutoReplyThread 自己的线程中执行，不会阻塞主线程，但 API 调用本身是阻塞的。如果多个弹幕短时间内匹配到同一规则，AutoReplyThread 会串行等待每个 API 响应。

**修改方法**: 
1. 缓存天气/房间信息等不频繁变化的数据（每 5-10 分钟更新一次即可）
2. 预热缓存，避免在处理消息时才去获取

---

## 优先级总结（基于你的实际流量：10人进入/秒 + 3弹幕/秒）

| 优先级 | 问题 | 影响 | 预计修复时间 |
|--------|------|------|------------|
| **P0 立刻修复** | 问题 1：主线程内同步 HTTP 请求 | **消息处理管线完全阻塞，所有功能瘫痪** | 30 分钟 |
| **P0 立刻修复** | 问题 2：主线程内 exec 打开浏览器 | **随机卡死 500-3000ms** | 5 分钟 |
| **P0 立刻修复** | 问题 3：每条进入创建新线程 | **每秒 10 个线程，系统资源耗尽** | 10 分钟 |
| **P1 尽快修复** | 问题 4：单线程消息处理 | 修复 P0 后可处理，但无扩展性 | 1 小时 |
| **P1 尽快修复** | 问题 8：OkHttp 无连接池 | 减少 TCP 握手延迟 | 5 分钟 |
| **P2 计划修复** | 问题 5、6：Vector + wait/notify | 减少不必要的锁竞争 | 2 小时 |
| **P2 计划修复** | 问题 7：日志每次打开/关闭 | 减少磁盘 I/O | 30 分钟 |
| **P2 计划修复** | 问题 12：AutoReply 缓存 API 结果 | 减少自动回复延迟 | 1 小时 |
| **P3 可选优化** | 问题 9、10、11 | 细节优化 | 碎片时间 |

---

## 修复顺序建议

**第一步（30 分钟，解决瘫痪）**: 
1. 移除主线程中的 `httpGetUserCardInfo` 调用，改为异步
2. 移除主线程和 `processFollowings` 中的 `Runtime.exec()` 调用
3. 将 `new Thread()` 改为固定线程池

**第二步（1 小时，提升吞吐）**:
4. 给 OkHttpClient 配置连接池
5. 将消息处理改为线程池异步模式
6. 替换 Vector 为 BlockingQueue

**第三步（2 小时，优化体验）**:
7. 日志批量写入
8. 缓存天气/房间信息 API 结果
9. WebSocket 异步发送

---

## 关键代码位置速查

| 问题 | 文件 | 行号 |
|------|------|------|
| 主线程 HTTP 请求 | `thread/core/ParseMessageThread.java` | 1106 |
| 主线程 exec 浏览器 | `thread/core/ParseMessageThread.java` | 1179 |
| 创建新线程 | `thread/core/ParseMessageThread.java` | 1209-1211 |
| processFollowings 中 exec | `http/HttpRoomData.java` | 492 |
| Vector 队列定义 | `conf/PublicDataConf.java` | 103-122 |
| wait/notify 模式 | `thread/core/ParseMessageThread.java` | 89, 113, 237, 1488 |
| 日志每次打开/关闭 | `tools/file/LogFileTools.java` | 39-113 |
| OkHttp 无连接池 | `utils/OkHttp3Utils.java` | 34-39 |
| WebSocket 同步发送 | `controller/DanmuWebsocket.java` | 56-63 |
| 忙等循环 | `thread/ParseThankGiftThread.java` | 57-67 |
| AutoReply API 调用 | `thread/AutoReplyThread.java` | 209-428 |
